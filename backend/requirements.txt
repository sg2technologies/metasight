# Project dependencies
#
# Pinned to exact versions (captured via `pip freeze` from a known-working
# venv) for reproducible installs — the previous unpinned list meant a fresh
# `pip install -r requirements.txt` could pull materially different major
# versions of FastAPI/SQLAlchemy/Celery/cryptography months apart, risking
# breaking changes on every redeploy. Bump versions deliberately, not by
# omission.
setuptools==83.0.0
fastapi==0.138.0
uvicorn[standard]==0.49.0
gunicorn==23.0.0        # production process manager (Linux/systemd only — see DEPLOYMENT.md)
SQLAlchemy==2.0.51
alembic==1.18.4
psycopg2-binary==2.9.12
python-jose[cryptography]==3.5.0
passlib[argon2]==1.7.4
argon2-cffi==25.1.0
pyotp==2.10.0           # TOTP MFA
qrcode[pil]==8.2         # MFA enrollment QR code
Authlib==1.8.0           # OIDC SSO (discovery, JWKS, state/nonce)
python-dotenv==1.2.2
pydantic-settings==2.14.2
celery[redis]==5.6.3
redis==6.4.0
cryptography==50.0.0
python-multipart==0.0.32
slowapi==0.1.10
sqlglot==30.11.0
# Core essentials
email-validator==2.3.0

# SQL Drivers
pymysql==1.2.0
pymssql==2.3.13
oracledb==4.0.1
snowflake-sqlalchemy==1.11.0
sqlalchemy-bigquery==1.17.0
duckdb-engine==0.17.0
clickhouse-connect==1.3.0
databricks-sql-connector==4.5.0   # pulls thrift>=0.23 (fixes PYSEC-2026-3925/3926/3927); 4.3.0's thrift<0.23 pin was the vulnerable one
trino==0.337.0
pyathena==3.32.0
vertica-python==1.4.0
pydruid==0.6.9
pinotdb==9.1.2

# NoSQL Drivers
pymongo==4.17.0
cassandra-driver==3.30.0
elasticsearch==9.4.1
opensearch-py==3.2.0

# Cloud Storage
boto3==1.43.36
azure-storage-blob==12.30.0
google-cloud-storage==3.12.0

# HTTP client (webhook alerts)
httpx==0.28.1

# PAM Platform
minio==7.2.20
hvac==2.4.0            # HashiCorp Vault client (optional — only needed if vault_path is set on a DataSource)
